{# total pages #}
{# {{ dump( search_pagination_page() ) }} #}

{# current page #}
{# {{ dump( search_pagination_pages() ) }} #}

{# Links #}
{# {{ dump( search_pagination_links() ) }} #}

{# items per page #}
{# {{ dump(search_pagination_per_page()) }}  #}


{% extends "@Search/pagination/base.html" %}